{% extends 'base.html.twig' %}

{% block body %}

	{% block menu %}
		<nav class="sidebar-navigation">
			<ul>
				<li>
					<a href="{{ url('user_edit') }}">
						<i class="fas fa-user-circle"></i>
						<span class="tooltip">{% if app.user %}{{ app.user.username }}{% endif %}</span>
					</a>
				</li>
				{% if is_granted('ROLE_USER') %}
					<li>
						<a href="{{ url('index_participant') }}">
							<i class="fas fa-list"></i>
							<span class="tooltip">Liste des participants</span>
						</a>
					</li>
					<li>
						<a href="{{ url('participant_add') }}">
							<i class="fas fa-user-plus"></i>
							<span class="tooltip">Ajout d'un participant</span>
						</a>
					</li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li>
						<a href="{{ url('user_list') }}">
							<i class="fas fa-address-book"></i>
							<span class="tooltip">Liste des utilisateurs</span>
						</a>
					</li>
				{% endif %}
				<li>
					<a href="{{ url('app_logout') }}">
						<i class="fas fa-power-off"></i>
						<span class="tooltip">Déconnexion</span>
					</a>
				</li>
			</ul>
		</nav>

	{% endblock %}

	<div class="navbar-wrapper">
		{% block page_content %}{% endblock %}
	</div>

{% endblock %}
